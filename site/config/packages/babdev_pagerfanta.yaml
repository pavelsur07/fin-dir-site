babdev_pagerfanta:
    # Страница за пределами списка -- 404, а не последняя страница (PATTERNS.md §10.5).
    exceptions_strategy:
        out_of_range_page: to_http_not_found
        not_valid_current_page: to_http_not_found
