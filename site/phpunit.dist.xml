<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="SHELL_VERBOSITY" value="-1" />

        <!-- env, а не server: в compose и CI APP_ENV приходит настоящей
             переменной окружения, а $_ENV у Symfony приоритетнее $_SERVER.
             Через server ядро поднималось бы в dev. -->
        <env name="APP_ENV" value="test" force="true" />

        <!-- Заменяет .env.test: файлы .env в git не хранятся. Без force
             реальное окружение (compose, CI) перекрывает эти значения. -->
        <env name="KERNEL_CLASS" value="App\Kernel" />
        <env name="APP_SECRET" value="test-secret" />
        <env name="DEFAULT_URI" value="https://vashfindir.ru" />
        <env name="DATABASE_URL" value="postgresql://site:secret@site-postgres:5432/site?serverVersion=17&amp;charset=utf8" />
    </php>

    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
    </extensions>
</phpunit>
