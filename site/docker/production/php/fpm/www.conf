[www]
; TODO: switch to dedicated app user/group once it exists in runtime image.
user = www-data
group = www-data

listen = 9000
clear_env = no

pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6

ping.path = /ping
pm.status_path = /status
