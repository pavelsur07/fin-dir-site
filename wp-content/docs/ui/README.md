# UI документация темы «Ваш ФинДиректор»

Эта папка содержит **единую систему UI-эталонов** проекта.
Документация используется как **источник истины** при разработке и поддержке темы.

---

## 🔹 Как читать эту документацию

### Визуальные эталоны
- `reference-v21.html` — утверждённый визуальный HTML-референс
- `anti-patterns-v1.html` — примеры того, как **делать нельзя**

### Формальные эталоны (обязательные к применению)
- `tokens.md` — цвета, типографика, базовые принципы
- `naming.md` — правила именования классов, файлов, изображений
- `buttons.md` — эталон кнопок (CTA)
- `sections.md` — правила построения секций и структуры страниц
- `layout.md` — правила базового layout (каркас страниц)
- `hero-home.md` — эталон hero главной страницы (Hero4, 1:1)
- `hero-service-pages.md` — эталон hero сервисных страниц (Hero3, wide 16:9)
- `cards.md` — правила карточек
- `forms.md` — правила форм
- `change-log.md` — история изменений UI
- `control-level.md` — эталон блока выбора сценария (vf-control-level)


---

## 🔹 Принципы проекта (обязательно)

1. В прод-коде **нет** `demo / test / tmp / draft` в классах, id, названиях файлов.
2. Любое изменение UI:
    - сначала фиксируется в `docs/ui/`
    - потом внедряется в код
    - фиксируется в `change-log.md`
3. Один UI-элемент = один эталон.
4. Стили UI должны быть изолированы (scoped) и не ломать другие страницы.

---

## 🔹 Приоритет документации

При конфликте:
1. `docs/ui/*.md`
2. `reference-v21.html`
3. код темы

Код **всегда** приводится в соответствие с документацией, а не наоборот.
